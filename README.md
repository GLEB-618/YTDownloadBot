# 🎬 YTDownloadBot

Telegram-бот для скачивания видео с YouTube  
(лучшее доступное качество → отправка пользователю)

---

## 📝 Описание

YTDownloadBot — это Telegram-бот, который:

- принимает **YouTube-ссылку**
- скачивает **лучшую видео- и аудио-дорожку** через `yt_dlp`
- склеивает их через `ffmpeg`
- отправляет пользователю итоговый MP4-файл
- работает на `Python 3.11`
- написан на `aiogram 3` + `yt_dlp`
- использует временные директории для хранения файлов
- поддерживает отправку файлов через **локальный Telegram Bot API**, чтобы обходить лимит 50 МБ

Проект полностью подходит для деплоя как в Docker, так и локально

---

# 📦 Установка и запуск

## 1. Установить Python 3.11

Проверь:

```bash
python3 --version
```

Если не 3.11 — установи нужную версию

---

## 2. Клонировать проект

```bash
git clone https://github.com/GLEB-618/YTDownloadBot.git
cd YTDownloadBot
```

---

## 3. Создать виртуальное окружение

```bash
python3.11 -m venv .venv
```

Активировать:

### Windows:

```bash
.venv\Scripts\activate
```

### Linux / macOS:

```bash
source .venv/bin/activate
```

---

## 4. Установить зависимости

```bash
pip install -r requirements.txt
```

## 5. Установка FFmpeg

Для корректной работы `yt_dlp` и обработки видео ботом требуется установленный **FFmpeg**.

Выберите инструкцию для вашей системы:

### 💻 Windows

#### Через **winget** (рекомендуется)

```bash
winget install ffmpeg
```

#### Через Chocolatey

```bash
choco install ffmpeg
```

### 🐧 Linux (Ubuntu / Debian)

```bash
sudo apt update
sudo apt install ffmpeg
```

### 🐧 Linux (Arch / Manjaro)

```bash
sudo pacman -S ffmpeg
```

### 🍏 macOS

#### Через Homebrew:

```bash
brew install ffmpeg
```

### Проверка установки

После установки проверьте, что ffmpeg доступен:

```bash
ffmpeg -version
```

Если выводится версия — всё работает.

---

# ⚙️ Настройка проекта

## 1. Настройка переменных окружения

В проекте есть файл:

```
.env.example
```

Скопируй его в:

```
.env
```

Команда:

```bash
cp .env.example .env
```

Открой `.env` и укажи данные:

```
BOT_TOKEN="СЮДА-ВСТАВЬ-ТОКЕН-БОТА"
TELEGRAM_API_BASE_URL="http://<ip_твоего_сервера>:8081"
LOG_LEVEL="INFO"
```

Если используешь официальный API Telegram (лимит 50 МБ), оставь:

```
TELEGRAM_API_BASE_URL=https://api.telegram.org
```

Если используешь локальный Bot API (см. ниже):

```
TELEGRAM_API_BASE_URL=http://localhost:8081
```

---

# 🚀 Запуск бота

```bash
python main.py
```

Логи будут писать, что бот запущен:

```
INFO: Бот запущен
```

---

# 📥 Локальный Telegram Bot API (обход лимита 50 МБ)

Telegram по умолчанию **не разрешает ботам загружать файлы > 50 МБ**, поэтому для больших видео нужно поднять **свой сервер Bot API**

## Получить API ID / API HASH

1. Перейдите на [https://my.telegram.org](https://my.telegram.org)
2. Войти по номеру телефона
3. Перейти в **API Development Tools**
4. Создать приложение
5. Получить:
    - `api_id`
    - `api_hash`

После получения `api_id` и `api_hash` вставьте их в `docker-compose.yaml`:

```yml
TELEGRAM_API_ID: "СЮДА-ВСТАВЬ-API-ID"
TELEGRAM_API_HASH: "СЮДА-ВСТАВЬ-API-HASH"
```

---

## Запуск Bot API

```bash
docker compose up -d
```

Проверка:

```bash
curl http://localhost:8081/bot<ТОКЕН>/getMe
```

Если видишь JSON → работает.

---

# 🎧 Как работает скачивание видео

YTDownloadBot использует:

- `yt_dlp` для загрузки видео
- формат: `"bestvideo+bestaudio/best"`
- итоговое объединение → `mp4`

То есть:

- скачивается **лучшая версия видео** (VP9/AV1/H.264)
- скачивается **лучшая дорожка аудио** (Opus/AAC)
- они объединяются через ffmpeg в один MP4

Также предусмотрена обработка:

- плейлистов
- временных директорий
- ошибок загрузки